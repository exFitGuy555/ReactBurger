const Aux = (props) => props.children;

export default Aux;


//just a components wrapper thats hold its own props.children
//there is a requirement of having a wrapping root elemnt when we creating the Layout , so Aux wull use us in this manner